server:
  port: 8081

logging:
  level:
    root: INFO

spring:
  application:
    name: producer-app
  datasource:
#    url: jdbc:mysql://dev.pcanabarro.com:3306/source_db
    url: jdbc:mysql://localhost:3306/source_db
    username: root
    password: ServerChacara@Root098!
  jpa:
    hibernate:
      ddl-auto: none
